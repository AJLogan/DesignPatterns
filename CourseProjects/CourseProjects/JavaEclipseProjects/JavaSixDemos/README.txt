---- Java DB Demo ----
# The 'derby.jar' file from '<JDK>/db/lib' needs to be in the classpath
# Queries can be run against a database via the 'ij' tool at '<JDK>/db/frameworks/embedded/bin/ij.bat'
# To create a database run 'ij' and type CONNECT 'jdbc:derby:CoursesDB;create=true';
